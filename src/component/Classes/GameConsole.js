export class GameConsole {
    constructor(title, hasGaranty, ports, system) {
        this.title = title;
        this.hasGaranty = hasGaranty;
        this.ports = ports;
        this.system = system;
    }
}

